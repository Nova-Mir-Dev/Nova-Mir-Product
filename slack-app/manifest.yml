_metadata:
  schema_version: 1
display_information:
  name: nova-mir-clients
  description: nova-mir-clients Slack bot
features:
  bot_user:
    display_name: nova-mir-clients
oauth_config:
  scopes:
    bot:
      - chat:write
      - commands
      - channels:history
settings:
  event_subscriptions:
    request_url: https://your-domain.com/api/slack/events
  interactivity:
    is_enabled: true
    request_url: https://your-domain.com/api/slack/interactive
